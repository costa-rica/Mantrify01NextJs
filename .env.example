# API Configuration
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Logging Configuration (per docs/LOGGING_NODE_JS_V06.md)
# Values: development | testing | production
NODE_ENV=development

# Application name (used for log file naming)
NAME_APP=Mantrify01NextJs

# Absolute path to logs directory
PATH_TO_LOGS=/absolute/path/to/logs

# Optional: Maximum size of each log file in megabytes (default: 5)
LOG_MAX_SIZE=5

# Optional: Number of rotated log files to retain (default: 5)
LOG_MAX_FILES=5
